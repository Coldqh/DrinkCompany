# Drink Company — рабочее название

Мобильная PWA-игра про производство пива и сидра, разработку рецептур, поиск покупателей и постепенное развитие компании.

## Стек

- React
- TypeScript
- Vite
- IndexedDB
- GitHub Pages
- ручной Service Worker без обязательного полного офлайна

## Запуск

```bash
npm install
npm run dev
```

## Проверки

```bash
npm test
npm run lint
npm run build
```

## GitHub Pages

Workflow `.github/workflows/deploy-pages.yml` при каждом push в `main`:

1. устанавливает зависимости через `npm ci`;
2. запускает тесты и статическую проверку;
3. собирает приложение в `dist`;
4. публикует сборку через GitHub Pages.

Один раз открой в репозитории **Settings → Pages** и выбери **Source: GitHub Actions**.

Адрес проекта после успешного развёртывания:

`https://coldqh.github.io/DrinkCompany/`

## Текущий вертикальный срез

- создание компании;
- стандартный и жёсткий режим;
- выбор реальной страны и региона;
- выбор объекта;
- списание стартовых и ежедневных расходов;
- переход времени по дням;
- мобильная навигация;
- локальное автосохранение;
- экспорт и импорт сохранения.

Следующий срез: оборудование и первая настройка рецептуры пива.
